# Base URL of the running xsl-backend Express server. No trailing slash.
# The backend already whitelists http://localhost:5173 (this app's Vite dev
# server) in its CORS config (xsl-backend/src/app.ts), so the default below
# works with zero backend changes.
VITE_API_BASE_URL=http://localhost:3000
